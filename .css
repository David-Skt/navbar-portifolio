* {
    margin: 0;
    padding: 0;
}
/** NAVBAR **/
a.logo, .sobre, .projetos, .contato, .home {
    color: white;
    text-decoration: none;
    transition: .5s;
}
a:hover{
    opacity: 0.7;
    color: 	#4F4F4F
}
nav { 
    display: flex;
    justify-content: space-around;
    align-items: center;
    font-family: Arial, Helvetica, sans-serif;
    background: #252525;
    height: 8vh;
}
.logo {
    font-size: 30PX;
    text-transform: uppercase;
    letter-spacing: 5px;
}
.nav-list {
    list-style: none;
    display: flex;
}
.nav-list li {
    font-size: 25px;
    letter-spacing: 3px;
    margin-left: 50px;
    transition: .1.5s;
}
.nav-list li:hover {
    font-size: 35px;
}
.menu div {
    width: 32px;
    height: 2px;
    background: white;
    margin: 8px;
    display: none;
}
.menu {
    cursor: pointer;
    transition:all 0.5s ease;
}
.menu:hover {
    opacity: 0.7;
    color: rgba(255, 255, 255, 0.39);
}
@media (max-width:800px){
    .nav-list {
        position: absolute;
        top: 8vh;
        right: 1;
        width: 100%;
        height: 92vh;
        background:#252525 ;
        flex-direction: column;
        align-items: center;
        text-align: center;
        display: none;
        font-size: 15px;
    }
    .logo {
        font-size: 25px;
    }
    .menu div {
        display: block;
    } 
    .open {
        display: flex ;
        justify-content: space-around ;
    }
}